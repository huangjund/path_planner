#pragma once

#include "GridState.h"

namespace HybridAStar {
namespace Common {
  double GridState::distance(const State *state1, 
                              const State *state2) const {
    const double *s1 = state1->
  }
}
}